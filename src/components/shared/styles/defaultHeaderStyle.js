import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  header: {
    borderBottomWidth: 1,
    height: 50,
    marginBottom: -20,
  },

  headerTitle: {
    fontWeight: '300',
    fontSize: 22,
  },
});
