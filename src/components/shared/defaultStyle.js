import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  component: {
    flex: 1,
  },

  rowIcon: {
    justifyContent: 'center',
    marginRight: 10,
  },

  rowIconImage: {
    width: 18,
    height: 18,
  },
});
